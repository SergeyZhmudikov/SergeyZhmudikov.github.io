import React from "react";
import ReactDOM from "react-dom";
import styles from '../src/app.css';

const element = ( < div className = "example" > Hello < /div>);
        ReactDOM.render(element, document.querySelector('.container'));